# 第52輪查核｜2026-09-01

## 本輪結論

有實質改善，但**不改主行程、不改 `地圖指南.html`**。

第 51 輪已把 9/11 `고짚 서면본점` 主要聯絡值收斂成 `0507-1401-0815`，但重新讀最高優先級現場主檔 `行程.md` 時，仍發現第一順位旁保留「舊／直撥資料可見 `051-807-0815`」。這會讓旅途中五個人可能直接誤撥，因此本輪把歷史值從現場主檔徹底移除。

## 1. `고짚` 現場聯絡值只留一組

目前現場只認：

- 店名：`고짚 서면본점`
- 地址：`부산 부산진구 부전로 64 1층`
- 主要電話：`0507-1401-0815`
- 星期五安全用餐值：16:00 後
- 五人：下午先處理座位／預約

`051-807-0815` 只屬歷史查核資訊，不再出現在 `行程.md` 的可執行段落。這符合第 40、41、51 輪既有規則：最高優先級現場主檔不能同時顯示兩組會讓人誤用的聯絡值。

## 2. 天氣重新查看

2026-09-01 06:00 的 KMA 中期頁目前仍只把旅行日涵蓋到 9/11；9/12 尚未進這版逐日表。9/7～9/11 沒有出現足以推翻主線的大雨／強風新訊號，因此本輪不重排日期、不新增雨備，也不為小幅數值變動改 HTML。

## 3. HTML 判斷

`地圖指南.html` 在第 51 輪已確認使用 `0507-1401-0815`，因此本輪不提前改 HTML。下一個集中節點仍是第 54 輪。

## 本輪修改

- 更新 `行程.md`：移除 `051-807-0815`，只保留 `0507-1401-0815`。
- 新增 `第52輪查核.md`。
- 更新 `迭代索引.md`：完成 52，下一輪 53，下一個 HTML 節點 54。
- 新增規則：淘汰的電話／地址不得以「舊值參考」留在最高優先級現場主檔。

## 來源

- 既有第 51 輪交叉查證結果：`고짚 서면본점` 主要電話 `0507-1401-0815`。
- KMA 2026-09-01 06:00 中期預報頁：目前旅行日逐日區間仍到 9/11。
